{
  "watch": {
    "serve": {
      "patterns": ["**", "!build", "!obj", "!api"],
      "extensions": "md,yml"
    }
  },
  "scripts": {
    "watch": "npm-watch serve",
    "serve": "docfx build --serve",
    "metadata": "docfx metadata",
    "build": "docfx build"
  },
  "devDependencies": {
    "npm-watch": "^0.11.0"
  }
}