{
  "watch": {
    "serve": {
      "patterns": ["**"],
	  "ignore": ["build", "obj", "api", "node_modules"],
      "extensions": "md,yml",
      "delay": 2500
    }
  },
  "scripts": {
    "watch": "npm-watch serve",
    "serve": "docfx build --serve",
    "metadata": "docfx metadata",
    "build": "docfx build"
  },
  "devDependencies": {
    "npm-watch": "^0.11.0"
  }
}